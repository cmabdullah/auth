pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/release' }
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = 'auth'
